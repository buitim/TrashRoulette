query getShow($genre: String) {
	Page(page: 1, perPage: 50) {
		media(
			genre: $genre
			status_in: RELEASING
			sort: POPULARITY_DESC
			isAdult: false
		) {
			id
			genres
			coverImage {
				extraLarge
			}
			title {
				romaji
				english
				native
			}
		}
	}
}
