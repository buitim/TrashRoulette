query getShow($showID: Int) {
	# Goblin Slayer | FOR DEBUG
	Media(id: $showID, type: ANIME) {
		id
		coverImage {
			large
			extraLarge
			medium
		}
		title {
			romaji
			english
			native
		}
	}
}
